# Reporting Framework Configuration

# Database Connection (Example - override in your application)
# db.url=jdbc:sqlserver://localhost:1433;databaseName=ReportingDB;encrypt=true;trustServerCertificate=true
# db.username=sa
# db.password=YourPassword

# Metadata Cache Settings
metadata.cache.enabled=true
metadata.cache.ttl.minutes=60

# Logging
logging.level.com.reporting.framework=DEBUG
